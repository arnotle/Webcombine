<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>รายการเครื่องจักร</title>
    <link rel="stylesheet" href="css/list.css"> <!-- ลิงก์ไฟล์ CSS -->
</head>
<body>
    <div class="container">
        <h1>รายการเครื่องจักร</h1>

        <!-- แสดงรายการเครื่องจักร -->
        <table class="machine-table">
            <thead>
                <tr>
                    <th>#</th>
                    <th>ชื่อรุ่น</th>
                    <th>รูปภาพ</th>
                </tr>
            </thead>
            <tbody>
                <% machines.forEach((machine, index) => { %>
                    <tr>
                        <td><%= index + 1 %></td>
                        <td><%= machines.name %></td>
                        <td><img src="/uploads/<%= machine.image %>" width="100" alt="<%= machine.name %>"></td>
                    </tr>
                <% }); %>
            </tbody>
        </table>

        <a href="/new" class="add-machine-btn">🔙 เพิ่มเครื่องจักรใหม่</a>
    </div>
</body>
</html>
